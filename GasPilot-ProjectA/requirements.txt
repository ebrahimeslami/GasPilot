# Core (required for EIA + PJM + training/forecast)
pandas>=2.2
numpy>=2.0
requests>=2.31
scikit-learn>=1.4
joblib>=1.3

# Optional (plots)
matplotlib>=3.8

# Optional (CPC raster workflow)
rasterio>=1.3
